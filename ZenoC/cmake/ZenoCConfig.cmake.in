@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(NOT WIN32)
    find_dependency(Threads)
endif()
if(@ZENO_CURL_ENABLED@)
    find_dependency(CURL)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/ZenoCTargets.cmake")
check_required_components(ZenoC)
